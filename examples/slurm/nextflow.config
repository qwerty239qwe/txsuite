process {
    executor = 'slurm'
    queue = 'general'
    resourceLimits = [cpus: 32, memory: 128.GB, time: 72.h]
    maxRetries = 2
}

executor {
    queueSize = 20
    submitRateLimit = '2 sec'
}

apptainer {
    enabled = true
    autoMounts = true
}
