# Spatial examples

`txsuite workflow spatial` expects user-owned FASTQs, a 10x-compatible reference,
and a tissue image. It invokes a separately installed and licensed Space Ranger.

`txsuite spatial analyze` accepts either a Space Ranger `outs/` directory or an
existing SpatialData `.zarr` directory.
